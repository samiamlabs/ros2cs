{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "detail": "Build ros2 workspace (default)",
            "type": "shell",
            "command": "/${workspaceFolder}/build.sh",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "cwd": "${workspaceFolder}/.."
            },
            "problemMatcher": "$gcc"
        }, 
    ]
}